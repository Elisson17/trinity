version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: web
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: production
      RAILS_MASTER_KEY: 10e856daa901523db5480e4487fe09b6
      JOB_CONCURRENCY: 2
    restart: always

  jobs:
    build:
      context: .
      dockerfile: Dockerfile
      target: jobs
    environment:
      RAILS_ENV: production
      RAILS_MASTER_KEY: 10e856daa901523db5480e4487fe09b6
      JOB_CONCURRENCY: 2
    restart: always
