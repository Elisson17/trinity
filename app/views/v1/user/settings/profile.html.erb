<div class="p-6">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Meu Perfil</h1>
  </div>

  <% if notice %>
    <div class="mb-6 p-4 bg-green-50 border border-green-200 rounded-lg">
      <p class="text-green-800"><%= notice %></p>
    </div>
  <% end %>

  <% if @user.errors.any? %>
    <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
      <div class="text-red-800">
        <h4 class="font-medium mb-2"><%= pluralize(@user.errors.count, "erro") %> encontrado<%= "s" if @user.errors.count > 1 %>:</h4>
        <ul class="list-disc list-inside space-y-1">
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <%= form_with model: @user, url: v1_user_profile_path, method: :patch, local: true, class: "space-y-6" do |form| %>
    <!-- Avatar Section -->
    <div class="flex items-center space-x-6">
      <div class="w-20 h-20 bg-gray-300 rounded-full flex items-center justify-center">
        <span class="text-2xl font-medium text-gray-600">
          <%= @user.name.first.upcase %>
        </span>
      </div>
      <div>
        <h3 class="text-lg font-medium text-gray-900"><%= @user.name %></h3>
        <p class="text-sm text-gray-500">Membro desde <%= @user.created_at.strftime("%B de %Y") %></p>
        <!-- TODO: Adicionar funcionalidade de upload de avatar quando implementar Active Storage para users -->
        <button type="button" class="mt-2 text-sm text-blue-600 hover:text-blue-500 font-medium">
          Alterar foto
        </button>
      </div>
    </div>

    <hr>

    <!-- Profile Information -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <%= form.label :name, "Nome completo", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= form.text_field :name, 
            class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      </div>

      <div>
        <%= form.label :email, "E-mail", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= form.email_field :email, 
            readonly: true,
            class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm bg-gray-50 text-gray-500 cursor-not-allowed" %>
        <p class="mt-1 text-xs text-gray-500">O e-mail não pode ser alterado</p>
      </div>

      <div>
        <%= form.label :phone_number, "Telefone", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= form.telephone_field :phone_number, 
            placeholder: "(11) 99999-9999",
            class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      </div>

      <div>
        <label class="block text-sm font-medium text-gray-700 mb-2">Status da conta</label>
        <div class="flex items-center space-x-2">
          <% if @user.confirmed? %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
              <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              Verificada
            </span>
          <% else %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
              <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
              </svg>
              Pendente verificação
            </span>
          <% end %>
        </div>
      </div>
    </div>

    <hr>

    <!-- Account Settings -->
    <div>
      <h3 class="text-lg font-medium text-gray-900 mb-4">Configurações da Conta</h3>
      <div class="space-y-4">
        <div class="flex items-center justify-between">
          <div>
            <h4 class="font-medium text-gray-900">Alterar senha</h4>
            <p class="text-sm text-gray-500">Última alteração há mais de 90 dias</p>
          </div>
          <%= link_to edit_user_password_path, 
              class: "px-4 py-2 text-sm font-medium text-blue-600 bg-blue-50 rounded-md hover:bg-blue-100 transition-colors" do %>
            Alterar senha
          <% end %>
        </div>
      </div>
    </div>

    <div class="flex justify-end space-x-3 pt-6">
      <%= link_to v1_root_path, 
          class: "px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50 transition-colors" do %>
        Cancelar
      <% end %>
      <%= form.submit "Salvar alterações", 
          class: "px-4 py-2 text-sm font-medium text-white bg-blue-600 rounded-md hover:bg-blue-700 transition-colors" %>
    </div>
  <% end %>
</div>
