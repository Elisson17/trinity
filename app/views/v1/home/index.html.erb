<div class="max-w-md mx-auto bg-white p-8 rounded shadow">
  <h1 class="text-2xl font-bold mb-6 text-center">Bem-vindo, <%= current_user.name %>!</h1>
  <p class="text-gray-600 mb-6 text-center">Você está logado como <strong><%= current_user.email %></strong>.</p>
  
  <div class="flex justify-center">
    <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "bg-red-500 text-white px-4 py-2 rounded" %>
  </div>
</div>